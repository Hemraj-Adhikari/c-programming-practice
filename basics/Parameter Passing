#include <stdio.h>

// function with parameters
void add(int a, int b) {
    int sum = a + b;
    printf("Sum = %d\n", sum);
}

int main() {
    int x = 5, y = 10;
    add(x, y);  // passing parameters
    return 0;
}
